server:
  port: 8085

spring:
  config:
    import: optional:classpath:application-local.yml   # Projektspezifische Konfiguration (nicht im Git)

app:
  trello:
    api-key: ${TRELLO_API_KEY}
    api-token: ${TRELLO_API_TOKEN}
    board-id: ${TRELLO_BOARD_ID}
    base-url: https://api.trello.com/1
    poll-interval-ms: 30000
    # Listennames, Label-Namen, Custom Fields: in application-local.yml konfigurieren

  claude-code:
    timeout-minutes: 30      # maximale Laufzeit pro Claude-Code-Aufruf
    max-retries: 5            # maximale Wiederholungsversuche bei unvollständigem Ergebnis
    rate-limit-wait-minutes: 240   # Wartezeit nach Token-Erschöpfung (4 Stunden)
    # repo-path, repos: in application-local.yml konfigurieren

logging:
  level:
    com.example.trelloclaudebot: DEBUG
  file:
    name: ${LOG_FILE:logs/trello-claude-bot.log}
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 14
      file-name-pattern: ${LOG_FILE:logs/trello-claude-bot}.%d{yyyy-MM-dd}.%i.log
